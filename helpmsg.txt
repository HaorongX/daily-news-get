dng [opnion][arguments]
	wituout arguments:
		Equal to "--help"
	with arguments:
		-v Print version
		install name program [-a "arguments to script"] explain [-a "arguments to script"]
			Install a script
		remove name
			Uninstall a script (With delete its program)
		open [name]
			Turn a script status into 'open'
		close [name]
			Turn a script status into 'close'
		-c name
			Get the calling command
		---help
			type help infomation
